#version 430 core

uniform sampler2D 	mainTex;

uniform vec3	cameraPos;

in Vertex
{
	vec2 texCoord;
} IN;

out vec4 fragColor;

void main(void)
{
	//float alpha = texture(mainTex, IN.texCoord).a;
	//if (alpha < 0.5) {
	//    discard;
	//}
}